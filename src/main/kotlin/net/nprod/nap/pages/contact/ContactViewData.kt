package net.nprod.nap.pages.contact

/**
 * Data class for the contact page
 *
 * The contact page is static content, so the only thing that varies is the page title.
 */
data class ContactViewData(
    val title: String = "Contact"
)
